# Yaksha (ksp-saathi) test dependencies.
#
# Install:
#   pip install -r app/tests/requirements-test.txt
#
# Pin to the latest minor known to work with our Python 3.11 baseline.

pytest>=8.0,<9.0
pytest-asyncio>=0.23,<1.0
pytest-cov>=5.0,<7.0
httpx>=0.27,<1.0

# httpx pulls these transitively but pinning here protects CI from surprise
# upgrades that break our SSE parsing.
anyio>=4.0
sniffio>=1.3
